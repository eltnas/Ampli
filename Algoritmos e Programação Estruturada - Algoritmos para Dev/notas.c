#include <stdio.h>

int main() {
	int qtd_notas = 0, opcao;
	float notas, media, soma_notas = 0.0;
	
	do{
		printf("\nDigite a nota do aluno %d: ", qtd_notas + 1);
		scanf("%f", &notas);
		
	}
}